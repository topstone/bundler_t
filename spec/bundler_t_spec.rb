# frozen_string_literal: true

RSpec.describe BundlerT do
  it "has a version number" do
    expect(BundlerT::VERSION).not_to be_nil
  end

  # it format("does something useful", 1) do
  #  expect(false).to be(true)
  # end
end
